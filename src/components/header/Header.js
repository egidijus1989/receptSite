import Navigation from "../navigation/Navigation";

const Header = () =>{
    return(
        <header>
            <div className="container">
                <Navigation/>
            </div>
        </header>
    );
}
export default Header